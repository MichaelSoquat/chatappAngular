export enum Sidenav {
    OPEN = 'open',
    CLOSED = 'closed'
}

export enum PublicChannel {
    OPEN = 'open',
    CLOSED = 'closed'
}

export enum PrivateChannel {
    OPEN = 'open',
    CLOSED = 'closed'
}